interface:
  display_name: "Quote Card Builder"
  short_description: "Quote card fedeli alla fonte e al brand"
  default_prompt: "Usa $quote-card-builder per trovare e verificare la frase più forte, revisionarla nel Visual Review Studio e produrre in PNG tutti i formati oppure il solo rapporto scelto, senza alterarne attribuzione e significato."
policy:
  allow_implicit_invocation: true
